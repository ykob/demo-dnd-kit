import { Container, Header } from '~/layouts';
import { SortableSection } from '~/sections';

export default function App() {
  return (
    <Container>
      <Header />
      <SortableSection />
    </Container>
  );
}
